server {
    listen [::]:8080 default_server;
    listen 8080 default_server;
    server_name _;
    port_in_redirect off;

    root /var/lib/nginx/html;
    index index.html;

    #add_header Access-Control-Allow-Origin *;

    location /en/health-and-social-services {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/sosiaali-ja-terveyspalvelut {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/social-och-halsovardstjanster {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/health-and-social-services {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/kaupunkiymparisto-ja-liikenne {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/stadsmiljo-och-trafik {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/urban-environment-and-traffic {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /en/urban-environment-and-traffic {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp-varnish:6081;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /en/decision-making {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/paatoksenteko-ja-hallinto {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/beslutsfattande-och-forvaltning {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/administration {
      proxy_redirect     off;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

}
