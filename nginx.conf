server {
    listen [::]:8080 default_server;
    listen 8080 default_server;
    server_name _;
    port_in_redirect off;

    root /var/lib/nginx/html;
    index index.html;

    add_header Access-Control-Allow-Origin *;

    location /en/social-healthcare {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/sosiaali-ja-terveyspalvelut {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/social-och-halso {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/social-and-health {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-sote.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/kartat-ja-liikenne {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/kartor-och-trafik {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/maps {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /en/maps-and-transport {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://helfi-kymp.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /en/administration {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /fi/kaupunki-ja-hallinto {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /sv/stad-och-forvaltning {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

    location /ru/administration {
      proxy_redirect     off;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_pass http://strategia.docker.so:8080;
      proxy_hide_header 'Access-Control-Allow-Origin';
    }

}
